<isif condition="${product.shortDescription}">
    <div class="col-12 availability" id="collapsible-general-${loopState && loopState.count ? loopState.count : '1'}">
        <isprint value="${product.shortDescription}" encoding="off" />
    </div>
</isif>