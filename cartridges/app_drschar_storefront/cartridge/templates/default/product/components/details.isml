<isif condition="${product.longDescription}">
    <div class="col-12 content" id="collapsible-general-${loopState && loopState.count ? loopState.count : '1'}">
        <isprint value="${product.longDescription}" encoding="off" />
    </div>
</isif>
